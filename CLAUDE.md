# 미니게임 허브 프로젝트

아이들이 가볍게 즐기는 모바일 웹 게임 모음. Phaser 3 + PWA, 게임마다 독립 폴더, 루트 `index.html`이 허브(게임 선택 화면).

여러 환경(회사/집)에서 이어서 작업하는 프로젝트입니다.

## 세션 시작 시
1. `git pull` 먼저 실행 (안전, 변경 없어도 무해)
2. `세션로그.md`의 가장 최근 1~2개 항목만 읽기 (전체 다 읽지 말 것 — 로그가 길어져도 가볍게 유지하기 위함)

## 세션 종료 시
사용자가 "퇴근해야겠다", "집에서 하자", "회사가서 하자", "오늘은 그만하고 내일하자", "나머지는 이어서 하자" 등 — 자연어로 "다른 곳에서 이어서 하겠다"는 뜻을 표현하면, 별도 명령 없이 자동으로:
1. 이번 세션에서 무엇을 논의/결정/구현했는지 `세션로그.md`에 날짜와 함께 추가
2. `git add` + commit + push
3. 커밋 해시를 로그에 남기기

## 프로젝트 단축어
- "깃 그거 해줘" = 변경사항 commit + push (커밋 메시지는 알아서 작성)

## 배포
- GitHub repo: https://github.com/shinetoto1-pixel/sozoopakidsgame
- main 브랜치 push → Vercel 자동 배포 → https://sozoopakidsgame.vercel.app/
- **중요**: 새 게임은 완성/테스트 전까지 루트 `js/games.js`에 등록하지 말 것 (등록 안 하면 배포는 되어도 허브 UI에서 안 보여서 실사용자에게 노출 안 됨 — 미완성 게임을 안전하게 push해서 기기 간 이어가기 할 때 씀). 사용자가 실제로 플레이해보고 괜찮다고 확인하기 전에는 게임 목록에 추가하거나 최종 push하지 않기.

## 서비스워커 캐시 주의
게임/허브 파일을 수정하면 각 폴더의 `sw.js`에서 `CACHE_NAME` 버전을 올릴 것 (예: `-v1` → `-v2`). 안 올리면 이미 방문한 사용자 브라우저에 캐시된 옛날 버전이 계속 보임 (network-first 전략이라 온라인이면 자동으로 최신을 받아오긴 하지만, 새로고침을 한두 번 해야 완전히 갱신됨).

## 게임 목록
- `bubble-shooter` — 버블 슈터 (배포됨)
- `rabbit-catch` — 토끼 잡기 (배포됨)
- `worm-game` — 꼬물꼬물 지렁이, 난이도 1~5 선택 가능 (배포됨)
- `card-match` — 짝꿍 찾기 (카드 매칭, 배포됨). 실패 조건 없음, 5단계 순차 진행(3/4/6/8/10쌍), 매 단계 소재 랜덤(동물/과일/탈것/바다생물/디저트/우주), 새로고침해도 진행 단계 유지(localStorage)
- `poop-dodge` — 똥벼락을 피해라! (배포됨). 캐릭터(토끼/티라노 선택) 좌우로 움직여서 떨어지는 좋은 것 받기 + 똥 피하기. 실패(게임오버) 없음 — 똥 맞으면 1초 어지러움+20점 감점만. 150초 동안 8단계로 난이도 상승, 좋은 아이템/똥 스폰 주기를 완전히 분리해서 관리(똥은 후반부로 갈수록 훨씬 자주+커짐)
- `tank-game` — 탱크 대작전. **보류함** (아이들이 실제로 해보니 기존 3개 게임도 이미 어려워해서, 이것보다 훨씬 복잡한 탱크 게임은 접기로 함). 코드는 폴더에 남아있지만 허브 미등록 상태 유지.

## 참고: 타겟 연령대
실제 플레이 테스트 결과 아이들이 기존 게임들(버블슈터/토끼잡기/지렁이)도 어려워함 — 반응속도나 정교한 조작이 필요한 게임보다는 실패 조건이 없고 느긋한 게임이 더 잘 맞음. 새 게임 추천/설계할 때 이 점 감안할 것 (짝꿍 찾기가 이 교훈을 반영해서 만든 첫 게임).

# 세션 로그

## 2026-08-13

**이번 세션에서 한 일:**

1. **게임 허브 구축** — 루트 `index.html`이 게임 선택 화면. `js/games.js` 배열에 게임 정보(title/desc/emoji/color/path) 추가하면 카드가 자동 생성됨. 각 게임은 자기 폴더 안에 독립적인 Phaser 3 + PWA (manifest.json + sw.js)로 존재하고, 왼쪽 아래 🏠 버튼으로 허브에 복귀.

2. **방울 슈터** (`bubble-shooter/`) — 완성, 배포됨. 드래그 조준 + 매치3 버블슈터.

3. **토끼 잡기** (`rabbit-catch/`) — 완성, 배포됨. 구멍에서 나오는 토끼(핑크)/골든핑크(고득점)를 탭해서 잡고, 두더지는 피하는 방식. 45초 라운드제.

4. **꼬물꼬물 지렁이** (`worm-game/`) — 완성, 배포됨. 손가락 따라가기 조작. 난이도 1~5 선택 가능(시작화면에서 숫자 버튼), 4단계가 원래 기본 속도.
   - **버그였던 것**: 처음 만들었을 때 몸통 마디가 전부 머리 위치에서 시작해서 첫 이동에 즉시 "자기 몸 충돌"로 게임오버 나던 버그 있었음 → path를 미리 뒤로 펼쳐서 초기화하도록 수정해서 해결됨. (이런 종류의 "시작하자마자 즉사" 버그는 새 게임 만들 때마다 조심할 것.)

5. **서비스워커 캐시 버그 발견 및 전체 수정** — cache-first 전략 때문에 새로 배포해도 사용자 브라우저에 예전 버전이 계속 뜨는 문제 있었음. 허브+게임 4개 전부 network-first 전략(온라인이면 항상 최신 받아옴, 오프라인일 때만 캐시 사용)으로 교체함. **앞으로 새 게임 폴더 만들 때도 이 network-first 패턴으로 sw.js 작성할 것** (다른 게임의 sw.js 그대로 복사해서 CACHE_NAME만 바꾸면 됨).

6. **탱크 대작전** (`tank-game/`) — **작업 중, 미완성, 미배포, 허브에 등록 안 함**.
   - 컨셉: 배틀시티 스타일. 조이스틱(이동) + 별도 발사 버튼(둘 다 필요, 자동발사 아님). 13x13 격자, 정사각형 전장을 화면 위쪽에 배치하고 아래쪽을 조작 영역으로 씀.
   - 구현된 것: 5단계 맵 템플릿(기본형/미로형/요새형/개방형/결전형, 좌우대칭 디자인), 기지 방어(파괴되면 게임오버), 벽돌/강철 벽, 적 탱크 4종(기본/빠름/장갑3방/별탱크-별확정드롭), 별 파워업으로 플레이어 레벨 1~4 (레벨4는 강철 파괴 가능 + 이동속도 살짝 증가), 죽으면 레벨 다운 + 목숨 감소, 5단계 클리어 후 난이도 올리며 무한 반복.
   - 코드는 `tank-game/js/game.js` 하나에 다 있음. 로컬 서버(`http://localhost:8765/tank-game/index.html`)에서 로드 확인 + 기본 흐름(시작/이동/발사) JS로 직접 호출해서 에러 없음은 확인했지만, **제대로 된 테스트는 아직 안 함** (충돌 판정, 스테이지 클리어 흐름, 좁은 통로 이동감 등 실제 플레이 검증 필요).
   - 다음 세션에서 할 일: 스크린샷 도구로 실제 화면 확인 + 조이스틱/발사 조작감 검증, 충돌 관련 버그 있는지 꼼꼼히 확인(과거 지렁이 게임 즉사 버그처럼 조용히 숨어있을 수 있음), 다 됐으면 루트 `js/games.js`에 등록하기 전에 **사용자가 먼저 플레이해보고 괜찮다고 할 때까지 기다릴 것** (허브 등록 + 최종 push는 사용자 승인 후).

**커밋**: (아래 참고)
